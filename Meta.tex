%! Author = martin
%! Date = 29.06.21


% Letter
\recipient{Company Recruitment team}{Company, Inc.\\Somestreet 123\\Somestadt}
\date{\today}

% Curriculum Vitae - Personal Data
\name{Max}{Mustermann}
\title{Zusammenfassender Titel} % Optional, wenn nicht gewünscht, einfach die Zeile enfernen / auskommentieren
% \born{\date{04. Juli 1776}} % Optional, wenn nicht gewünscht, einfach die Zeile enfernen / auskommentieren %TODO WORKAROUND https://github.com/moderncv/moderncv/issues/62
\address{Musterstr. 42}{53123 Bonn}{Germany} % Optional, wenn nicht gewünscht, einfach die Zeile enfernen / auskommentieren; the "postcode stadt" and "country" arguments can be omitted or provided empty
\phone[mobile]{+1~(234)~567~890} % Optional, wenn nicht gewünscht, einfach die Zeile enfernen / auskommentieren; the optional "type" of the phone can be "mobile" (default), "fixed" or "fax"
\phone[fixed]{+2~(345)~678~901}
\phone[fax]{+3~(456)~789~012}
\email{max@mustermann.org} % Optional, wenn nicht gewünscht, einfach die Zeile enfernen / auskommentieren
\homepage{https://www.maxmustermann.com} % Optional, wenn nicht gewünscht, einfach die Zeile enfernen / auskommentieren

\extrainfo{Zusätzliche Informationen} % Optional, wenn nicht gewünscht, einfach die Zeile enfernen / auskommentieren
\photo[64pt][0.4pt]{bilder/Profilfoto} % Optional, wenn nicht gewünscht, einfach die Zeile enfernen / auskommentieren; '64pt' is the height the picture must be resized to, 0.4pt is the thickness of the frame around it (put it to 0pt for no frame) and 'picture' is the name of the picture file
\quote{Irgendein Zitat} % Optional, wenn nicht gewünscht, einfach die Zeile enfernen / auskommentieren

% Curriculum Vitae - Social Data
\social[linkedin]{max.mustermann} % Optional, wenn nicht gewünscht, einfach die Zeile enfernen / auskommentieren
\social[xing]{max_mustermann} % Optional, wenn nicht gewünscht, einfach die Zeile enfernen / auskommentieren
\social[twitter]{mmustermann} % Optional, wenn nicht gewünscht, einfach die Zeile enfernen / auskommentieren
\social[github]{mmustermann} % Optional, wenn nicht gewünscht, einfach die Zeile enfernen / auskommentieren
\social[gitlab]{mmustermann} % Optional, wenn nicht gewünscht, einfach die Zeile enfernen / auskommentieren
\social[stackoverflow]{0000000/maxmustermann} % Optional, wenn nicht gewünscht, einfach die Zeile enfernen / auskommentieren
\social[bitbucket]{mmustermann} % Optional, wenn nicht gewünscht, einfach die Zeile enfernen / auskommentieren
\social[skype]{mmustermann} % Optional, wenn nicht gewünscht, einfach die Zeile enfernen / auskommentieren
\social[orcid]{0000-0000-000-000} % Optional, wenn nicht gewünscht, einfach die Zeile enfernen / auskommentieren
\social[researchgate]{mmustermann} % Optional, wenn nicht gewünscht, einfach die Zeile enfernen / auskommentieren
\social[researcherid]{mmustermann} % Optional, wenn nicht gewünscht, einfach die Zeile enfernen / auskommentieren
\social[telegram]{mmustermann} % Optional, wenn nicht gewünscht, einfach die Zeile enfernen / auskommentieren
\social[whatsapp]{12345678901} % Optional, wenn nicht gewünscht, einfach die Zeile enfernen / auskommentieren
\social[signal]{12345678901} % Optional, wenn nicht gewünscht, einfach die Zeile enfernen / auskommentieren
\social[matrix]{@maxmustermann:matrix.org} % Optional, wenn nicht gewünscht, einfach die Zeile enfernen / auskommentieren
\social[googlescholar]{googlescholarid} % Optional, wenn nicht gewünscht, einfach die Zeile enfernen / auskommentieren